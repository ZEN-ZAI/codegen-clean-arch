import { createFlexibleContext } from "@/shared/utils/GenericProvider";
import useFind{{pascalCase name}} from "../hooks/useFind{{pascalCase name}}";

export const { Provider: {{pascalCase name}}Provider, useContext: use{{pascalCase name}}Context } = createFlexibleContext(useFind{{pascalCase name}});
